import {sqliteTable,text,integer,index,uniqueIndex,real} from 'drizzle-orm/sqlite-core';
export const organizations=sqliteTable('organizations',{id:text('id').primaryKey(),name:text('name').notNull(),createdAt:text('created_at').notNull()});
export const members=sqliteTable('members',{id:text('id').primaryKey(),orgId:text('org_id').notNull().references(()=>organizations.id),userId:text('user_id'),email:text('email').notNull(),role:text('role').notNull(),clientId:text('client_id')},t=>[uniqueIndex('idx_members_org_email').on(t.orgId,t.email),index('idx_members_user').on(t.userId),index('idx_members_email').on(t.email)]);
export const clients=sqliteTable('clients',{id:text('id').primaryKey(),orgId:text('org_id').notNull().references(()=>organizations.id),name:text('name').notNull(),data:text('data').notNull(),createdAt:text('created_at').notNull()},t=>[index('idx_clients_org').on(t.orgId)]);
export const entries=sqliteTable('entries',{id:text('id').primaryKey(),clientId:text('client_id').notNull().references(()=>clients.id,{onDelete:'cascade'}),kind:text('kind').notNull(),title:text('title').notNull(),status:text('status').notNull(),priority:text('priority').notNull().default('Medium'),owner:text('owner').notNull().default('Unassigned'),dueDate:text('due_date').notNull().default(''),projectId:text('project_id'),relatedId:text('related_id'),data:text('data').notNull(),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[index('idx_entries_client_kind').on(t.clientId,t.kind),index('idx_entries_client_due').on(t.clientId,t.dueDate)]);
export const analytics=sqliteTable('analytics',{id:text('id').primaryKey(),clientId:text('client_id').notNull().references(()=>clients.id,{onDelete:'cascade'}),date:text('date').notNull(),data:text('data').notNull(),source:text('source').notNull()},t=>[uniqueIndex('idx_analytics_client_date').on(t.clientId,t.date)]);
export const auditLogs=sqliteTable('audit_logs',{id:text('id').primaryKey(),orgId:text('org_id').notNull().references(()=>organizations.id),userId:text('user_id').notNull(),action:text('action').notNull(),resource:text('resource').notNull(),createdAt:text('created_at').notNull()},t=>[index('idx_audit_org_date').on(t.orgId,t.createdAt)]);
export const rateLimits=sqliteTable('rate_limits',{id:text('id').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});

export function parseCsv(text:string){const rows:string[][]=[];let row:string[]=[],cell='',quote=false;for(let i=0;i<text.length;i++){const ch=text[i];if(ch==='"'){if(quote&&text[i+1]==='"'){cell+='"';i++}else quote=!quote}else if(ch===','&&!quote){row.push(cell);cell=''}else if((ch==='\n'||ch==='\r')&&!quote){if(ch==='\r'&&text[i+1]==='\n')i++;row.push(cell);if(row.some(v=>v.trim()))rows.push(row);row=[];cell=''}else cell+=ch}if(quote)throw Error('A quoted cell is not closed.');if(cell||row.length){row.push(cell);rows.push(row)}const headers=rows.shift()?.map(h=>h.replace(/^\uFEFF/,'').trim());if(!headers?.length||new Set(headers).size!==headers.length)throw Error('The first row must have unique column names.');if(rows.length>500)throw Error('Import up to 500 rows at a time.');return rows.filter(r=>r.some(x=>x.trim())).map((r,i)=>{if(r.length!==headers.length)throw Error('Row '+(i+2)+' does not match the header columns.');return Object.fromEntries(headers.map((h,j)=>[h,r[j].trim()]))})}
